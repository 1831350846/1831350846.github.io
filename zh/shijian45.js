        function furtherProcess(shi) {
            // 获取输入框的值（人数）45表
            let numericValue = parseInt(shi, 10);
            let acceptance = numericValue - 1;
                return acceptance;

      
        }
